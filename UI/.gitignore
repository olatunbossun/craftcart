# Dependencies
# These folders contain all the installed packages for your project.
# They can be very large and should be re-installed using 'npm install'.
/API/node_modules
/UI/node_modules

# Build Output
# This is the compiled code generated by Webpack.
# It should be rebuilt from the source code and not stored in Git.
/UI/public/app.bundle.js
/UI/public/vendor.bundle.js

# Environment Variables
# These files contain sensitive information like database credentials and API keys.
# They should never be committed to version control.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Log files
# Log files can become large and are specific to a user's machine.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# System files
# These are files generated by various operating systems and should be ignored.
.DS_Store
Thumbs.db

# Editor Directories and User-specific files
# These are configuration files for code editors like VS Code.
.vscode/